const AllImage = async () => {
  await new Promise((resolve) => setTimeout(resolve, 1000));
  // throw Error("oops");
  return (
    <div>
      <h1>All Image page</h1>
    </div>
  );
};

export default AllImage;
